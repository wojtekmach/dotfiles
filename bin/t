#!/bin/bash

if [ -f "$PWD/script/test" ]; then
    $PWD/script/test $*
else
    if [ -d $PWD/spec ]; then
        CMD="rspec --color"
    else
        CMD="testrb -Itest"
    fi

    if [ $(pidof zeus | wc -w) -gt 0 ]; then
        zeus $CMD $@
    else
        $CMD $@
    fi
fi
