#!/bin/bash
set -e

case $1 in
  "")
    iex -S mix
    ;;

  docs)
    shift
    if grep --quiet -E "ex_doc.*:docs" mix.exs; then
      MIX_ENV=docs mix docs -f html $@
    else
      mix docs -f html $@
    fi
    ;;

  *)
    mix $@
    ;;
esac
