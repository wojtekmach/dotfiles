#!/usr/bin/env ruby

if ARGV.length < 2
  puts "Usage: vbox-forward-port SERVICE_NAME PORT"
  exit 1
end

name = ARGV[0]
port = ARGV[1]

system "VBoxManage controlvm boot2docker-vm natpf1 '#{name},tcp,127.0.0.1,#{port},,#{port}"
