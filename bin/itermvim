#!/usr/bin/env bash
# osascript -e 'on run argv' -e "tell application \"iTerm2\" to tell current window to create tab with profile \"itermvim\" command \"vi $1 $2 $3 $4 $5\"" -e 'end run' > /dev/null
osascript -e 'on run argv' -e "tell application \"iTerm2\" to tell current window to create tab with profile \"Default\" command \"vi $1 $2 $3 $4 $5\"" -e 'end run' > /dev/null
