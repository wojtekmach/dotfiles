#!/bin/bash
set -ex

git clean -dfx
export MAKEFLAGS="-j$(getconf _NPROCESSORS_ONLN)"
time ./otp_build setup -a --with-ssl=$(brew --prefix openssl) --with-ssl-rpath=$(brew --prefix openssl)
make release
make release_docs DOCS_TARGETS=chunks
cd release/*
./Install -sasl $PWD
